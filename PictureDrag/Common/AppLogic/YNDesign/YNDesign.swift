//
//  YNDesign.swift
//  PictureDrag
//
//  Created by Ulian on 26.05.2023.
//

import UIKit

struct YNDesign {
    // use internal private(set) in case I need to make colors variable
    let colorBackgroundNormal = UIColor.systemBackground
    let colorBackgroundHighlighted = UIColor.systemGreen.withAlphaComponent(0.2)
    let borderColor = UIColor.systemGreen // CGColor(red: 76.0, green: 217.0, blue: 100.0, alpha: 1.0)
    let borderHighlightedColor = UIColor.systemGreen.withAlphaComponent(0.8)
    let imageBackgroundColor = UIColor.systemBlue.withAlphaComponent(0.2)
    let disabledButtonColor = UIColor.systemGray4
    let lightGrayBackground = UIColor.systemGray6
    let imageHighlighted = UIColor(red: 203/255, green: 237/255, blue: 207/255, alpha: 1) // 219/255
    
    // fonts for labels
}
